services:
  - type: web
    name: zisakuDAPyakasutamuGPTtukutta13666
    env: python
    plan: free
    region: singapore
    buildCommand: "python3 -m ensurepip --upgrade && python3 -m pip install --no-cache-dir --upgrade pip"
    startCommand: "python3 -m pip install --no-cache-dir flask openai requests && python3 app.py"
    autoDeploy: true
